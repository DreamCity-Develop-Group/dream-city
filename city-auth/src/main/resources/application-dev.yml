spring:
  devtools:
    restart:
      enabled: true
      additional-paths: src/main/java
      exclude: static/**
      additional-exclude: static/**,public/**

  cache:
    type: redis
  redis:
    host: 192.168.0.88
    port: 6379
    database: 0
    password: 123456
    timeout: 0
    ssl: false
    jedis:
      pool:
        max-active: 300
        max-wait: -1
        max-idle: 300
        min-idle: 1

